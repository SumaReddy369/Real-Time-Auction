# Server
PORT=8081

# PostgreSQL (use 5434 when running via docker-compose - ports mapped to avoid conflicts)
DB_HOST=localhost
DB_PORT=5434
DB_USER=auction
DB_PASSWORD=auction
DB_NAME=auction_db

# Redis (use 6381 when running via docker-compose)
REDIS_ADDR=localhost:6381
REDIS_DB=0
